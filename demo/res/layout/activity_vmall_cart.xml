<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#f6f6f6"
    android:orientation="vertical" >

    <include
        android:id="@+id/id_cart_title"
        layout="@layout/view_title" />

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:gravity="center" >

        <ImageView
            android:id="@+id/id_cart_basket"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="5dp"
            android:contentDescription="@string/app_name"
            android:src="@drawable/vmall_cart_basket" />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_below="@id/id_cart_basket"
            android:layout_marginTop="5dp"
            android:gravity="center"
            android:text="╯﹏╰ 购物车都饿扁了，快去喂它吧"
            android:textColor="#999999"
            android:textSize="16sp" />
    </RelativeLayout>

    <TextView
        android:id="@+id/id_cart_shop"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="30dp"
        android:layout_marginLeft="10dp"
        android:layout_marginRight="10dp"
        android:background="@color/vmall_cart_white_shape"
        android:text="去购物"
        android:gravity="center"
        android:padding="15dp"
        android:textSize="24sp" />

</LinearLayout>