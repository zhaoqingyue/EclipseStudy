<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:orientation="vertical"
    android:background="@color/windows_color" >

    <include
        android:id="@+id/id_friend_circle_title"
        layout="@layout/view_title" />

    <ListView
        android:id="@+id/id_friend_circle_listview"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@+id/id_friend_circle_title"
        android:divider="#00000000"
        android:dividerHeight="0dp"
        android:listSelector="#00000000"
        android:scrollbars="none" />

    <TextView
        android:id="@+id/id_friend_circle_nochat"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:drawableTop="@drawable/weinxin_chat_no_chat"
        android:gravity="center"
        android:text="@string/weixin_chat_no_msg"
        android:textColor="@color/black1"
        android:textSize="16sp"
        android:visibility="gone" />

</RelativeLayout>